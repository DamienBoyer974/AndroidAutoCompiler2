You have to set executable permission for start.sh & open terminal (CTRL + ALT + T) then type ./start.sh

We can say that the script is in an Alpha stage. In fact it works but I can't manage frames for the moment. So you need to restart the program to use different tabs.
